{
  "name": "postsnap",
  "private": true,
  "description": "PostSnap – one command to run API + frontend",
  "scripts": {
    "dev": "concurrently -n api,web \"npm run dev --prefix apps/api\" \"npm run web --prefix frontend\"",
    "dev:api": "npm run dev --prefix apps/api",
    "dev:web": "npm run web --prefix frontend",
    "dev:worker": "npm run worker --prefix apps/api",
    "dev:full": "concurrently -n api,worker,web \"npm run dev --prefix apps/api\" \"npm run worker --prefix apps/api\" \"npm run web --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
